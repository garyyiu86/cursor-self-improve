# For local merge only (Python 3.11 or 3.12 recommended — not 3.14)
transformers>=4.44.0
peft>=0.12.0
accelerate>=0.33.0
sentencepiece
protobuf>=5.26.1,<6
# Install torch separately from https://pytorch.org (CPU is OK for 1.5B merge):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
